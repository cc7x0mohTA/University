# A:

PUSH X
PUSH Y
MUL
PUSH Z
SUB      # SUB(MUL(X,Y),Z)
PUSH Z
PUSH Y
MUL
PUSH X
ADD      # ADD(X, MUL(Z,Y))
MUL
POP W

# B:

LOAD X
MUL Y
SUB Z
STORE $A1  # A1 = SUB(MUL(X,Y),Z)
LOAD Z
MUL Y
ADD X
MUL $A1
STORE W

# C :

MOVE $A1 X
MUL $A1 Y
SUB $A1 Z  # A1 = SUB(MUL(X,Y),Z)
MOVE $A2 Z
MUL $A2 Y
ADD $A2 X  # A2 = ADD(X, MUL(Z,Y))
MUL $A1 $A2
MOVE W $A1

# D :

MUL $A1 X Y
SUB $A1 $A1 Z # $A1 = SUB(MUL(X,Y),Z)
MUL $A2 Z Y
ADD $A2 $A2 X # $A2 = ADD(X, MUL(Z,Y))
MUL W $A1 $A2

